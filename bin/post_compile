#!/usr/bin/env bash

set -eo pipefail

indent() {
  sed "s/^/       /"
}

puts-step() {
  echo "-----> $@"
}

puts-step "Building frontend..."
cd frontend && yarn build | indent

puts-step "Cleaning up slug..."
rm -rf node_modules | indent
rm -rf frontend/node_modules | indent
