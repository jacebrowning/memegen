[tool.poetry]

name = "service"
version = "0.1.0"
description = ""
authors = ["Dan Lindeman <lindemda@gmail.com>"]

[tool.poetry.dependencies]

python = "^3.7"
requests = "^2.22.0"

[build-system]

requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
